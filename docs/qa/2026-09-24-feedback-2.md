# Замечания автора, 24.09.2026, вторая сборка — реестр

Как и в первом реестре: замечание, что было, что сделано, чем держится.
«Проверено автором» ставит автор после сборки на маке.

| # | Замечание | Что было | Что сделано | Чем держится | Статус |
|---|---|---|---|---|---|
| 1 | Окно выглядит не по-маковски: закрытие справа | Стандартное окно JUCE со своей нарисованной шапкой: кнопки справа, слева «Options» | Редактор при попадании в окно переключает его на системную шапку (`setUsingNativeTitleBar`). Светофор слева, полноэкранный режим, растягивание за края. «Options» убрана; выбор аудиоустройства перенесён в Настройки → Слух → «Аудиоустройство» | в контейнере не проверить (у инструментов нет окна); собирается в цели `EarTrainer` | проверить на маке |
| 2 | CI на Windows: `setenv` не найден | `setenv` есть только в POSIX | `_putenv_s` под `JUCE_WINDOWS` в `tools/ClickMap.cpp` и `tools/EditorSnapshots.cpp` | следующий прогон CI | готово |
| 3 | Размер окна — от экрана, без жёстких 940×620; тянется как обычное окно; убрать S/M/L/XL | Масштаб задавался списком, окно упиралось в пол 940×620 | Масштаб считается из размера окна: `min(ширина/1100, высота/780)`, шаг 1/40, пределы 0,8–2,4. Макет всегда получает не меньше 1100×780 логических точек. Первый запуск открывает окно на 88 % высоты экрана, дальше — с тем размером, с каким его закрыли. Список размеров удалён (ADR 042) | снимки при `SNAP_SIZE=1100x780` (самый тесный реальный случай) | проверить растягивание на маке |
| 4 | Правый верхний угол — целиком в настройки; обновления автоматически | Тема, обновления, размер, язык — иконками в полосе | Тема и язык — Настройки → Внешний вид. Обновления — Настройки → О программе: «По запросу / Автоматически» (по умолчанию автоматически) и «Проверить» с итогом под кнопкой. Угол пуст, в нём остались «Слух» и «Серия». Правило офлайна в CLAUDE.md обновлено (ADR 042) | снимки `SettingsAppearance`, `SettingsAbout`; ClickMap | готово |
| 5 | Всплывающие подсказки в Студии перекрывают обзор | Подсказка рисовалась поверх нижней части графика | У подсказки своя полоса под панелью ручек; график и ручки её не делят | снимки `StudioEQ/Comp/Verb` | готово |
| 6 | Уроки от приглашённых авторов для плагинов | — | Записано в `docs/roadmap.md` → «Recorded, not started»: формат урока как данные, проверка, пакеты как у звуков | — | на потом |
| 7 | Учитывать точность ответа | Верно = +1, неважно насколько | На непрерывных шкалах верный ответ даёт 1 очко плюс до 0,9 за точность (десятые). Показано в «+1,4» после ответа, в счёте забега и в итоге («Очки 14,1 — верно 11, рекорд 11»). Рекорды пока считаются в верных ответах, чтобы старые не обнулились | `SessionManagerTest`: «precision earns tenths…» | готово |
| 8 | Шрифты нечитаемы — уйдёт ли с масштабом? | Подписи и пояснения полусжатым Regular 13–14 px, цвет `textDim` | Гипотеза подтвердилась наполовину. Масштаб делает буквы крупнее на большом окне, но не толще. Поэтому label/caption переведены на Medium, `textDim` светлее (тёмная тема a0a0b0 → b0b0bf, светлая 6d6a64 → 5f5c56), шаги урока в Студии — основным цветом | снимки | проверить глазами на маке |
| 9 | При показе подсказки экран прыгает вверх | Карточки ответа ужимались, когда появлялась картинка | Место под картинку зарезервировано всегда, когда подсказки разрешены: до покупки и после покупки раскладка одна | сравнение `Zoned` / `HintAll2`, `Training` / `HintAll0` | готово |
| 10 | Технические параметры — на английском | Freq, Gain, Q, Threshold, Ratio, Bell, Low shelf, Room/Hall/Plate/Spring, Bypass переводились | 50 ключей (ручки, типы фильтров, типы реверберации, имена модулей, Bypass) на английском во всех 12 языках. Правило в CLAUDE.md расширено | аудит текста на 12 языках | готово |

## Что осталось открытым

- «Слабая / Сильная» (компрессия) и «Узко / Широко» (ширина стерео) — это
  ответы-описания, а не параметры плагина, поэтому переведены. Если нужно
  «Light / Heavy», «Narrow / Wide» — одна правка в таблицах.
- Проверка обновлений раз в запуск; если релизов нет или нет сети, она
  молчит.
